title: Ham Radio Notes
description: Notes on CW, SOTA, antennas, portable operation, and experiments
theme: minima
markdown: kramdown
kramdown:
  input: GFM

defaults:
  - scope:
      path: ""
    values:
      layout: default

header_pages:
  - about.md
  - topics.md
